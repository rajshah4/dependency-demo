[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dep-propagator"
version = "0.1.0"
description = "OpenHands Cloud V1 conversation orchestrator for dependency propagation"
requires-python = ">=3.11"
